name: release
on:
  release:
    types: [created]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Compile Shell (Linux)
        uses: LamkasDev/maple-build-ubuntu@main
      - name: Upload the artifacts (Linux)
        uses: skx/github-action-publish-binaries@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: 'maple-shell'
      - name: Compile Shell (Windows)
        uses: LamkasDev/maple-build-win@main
      - name: Upload the artifacts (Windows)
        uses: skx/github-action-publish-binaries@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: 'maple-shell.exe'